[launcher]
version = 1.0
description = Disables the search scheduler so that you can temporarily stop scheduled searches from running
author = LukeMurphey

[package]
id = disable_scheduler

[install]
build = ${value.build.number}
# Last commit: ${value.build.date}

[ui]
is_visible = false
label = Disable Search Scheduler